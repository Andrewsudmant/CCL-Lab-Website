# Test and regression fixtures only

The JSON records below are captured interface, schema, clustering and regression fixtures. They are not live discovery results, are not a public feed and must never be rendered into the public site while `config/site.yml` keeps Current Conversations `in-development` and `public_feed_enabled: false`.

The records remain here only because pipeline and schema tests exercise the future data contract. Public builds generate no fixture cards, fixture detail pages, item counts, filters, timestamps or JSON/RSS feed.
