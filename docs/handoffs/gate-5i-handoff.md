# Cities & Climate Learning Lab — Gate 5I handoff

## Outcome

Gate 5I finishes the bounded Draft 0.1 release corrections. Readiness is **READY_SUBJECT_TO_OWNER_MANUAL_KEYBOARD_CHECK**. Continue from the existing `codex/gate-5c-thematic-consistency` branch and PR #1, **Prepare the Draft 0.1 release candidate**. The archive manifest gives the exact ending commit. Do not rebuild this project from an empty repository or treat this handoff as source-code restoration.

The owner approved Gate 5H's public voice, homepage, theme examples, plain-first terminology, all 24 ideas, four Work page structures, Our Approach sequence and a public Draft 0.1 before Current Conversations is operational. Those decisions are now durable and must not be reopened.

## What changed and why

Only *Low carbon cities: is ambitious action affordable?* was removed from the prominent Delivery selection. Five examples remain. The paper itself, its bibliography/search presence, bibliographic metadata and primary Consequences/secondary Delivery relationships remain unchanged.

All 46 publication pages now use concise bibliographic details, explicit peer-review status, a short relationship label, original-source and correction links. The new `/publications/metadata-and-sources.html` explains sources, reconciliation, date precision, AI boundaries, statuses and corrections once. Thirty-five repeated generic relationship notes and 46 repeated procedural paragraphs disappear from individual pages. Ten approved descriptions appear once rather than twice; 36 generic procedural descriptions are withheld from visible prose, without invented replacements. Three distinctive chronology/collaboration notes remain. Publication identity and ordered authors/dates/identifiers have not changed.

## Challenges and their resolution

The first path assertions assumed absolute links; Quarto correctly rewrites them relative to the current route, so tests now resolve links rather than reject valid paths. The browser's native key-delivery wrapper cannot reliably show sequential Tab movement, even for native disclosure controls. We did not claim a keyboard pass: the archive contains a precise five-minute owner checklist. A real hidden-focus defect in navigation and two 320-pixel overflow issues were corrected and retested. Smooth scrolling also made stitched screenshots unreliable; settled viewport captures and complete local HTML provide the review evidence.

External checks were bounded and credential-free. LinkedIn refused the automated method. The Atlas main/about endpoints returned 503; records remain intact and the external availability warning is visible in the release assessment. Bluesky passed a minimal GET after a HEAD 404. A redirect response verifies only the starting endpoint. These limits were not disguised as successful end-to-end source verification.

## Evidence and transparent governance

A stale zero-byte Git index lock briefly blocked staging. Its age (18 minutes), lack of an open file handle and absence of any running Git process were checked before removing that exact lock. No index, working file or history was discarded.

Baseline: 162 tests, 87 pages. Final: 174 tests, 88 pages under both root and `/CCL-Lab-Website/`; internal links and static accessibility pass. The responsive matrix has 102 cases with no final overflow, broken images or recorded console errors. One known legacy-import deprecation warning remains. Repository/reachable-history secret scans found no matches for configured patterns.

Regression checks freeze the 46-record canonical inventory and all surviving selected-example objects. Full source notes, source links, dates, review state, prior decisions and correction history remain separate from lighter public presentation. The new owner-decision record and before/after table explain every bounded change. Historical audits are not overwritten; regenerated diagnostics receive Gate 5I filenames. Both archives identify their source commit, and the full review archive hashes each entry. No secrets, private data, provider bodies or build caches belong in either package; this compact handoff excludes rendered output.

PR #1 was updated and marked **ready for review**, not merged, after both Site checks runs passed on release-evidence commit `5e8245f` (runs `33414594027` and `33414590506`). The remote reported OPEN, MERGEABLE and CLEAN. This final handoff-record commit does not change the reviewed site; confirm Site checks again on the exact ending SHA in the manifest before merging, not only on that earlier green commit. Readiness is not owner approval. The final response records the final push/CI state. Current Conversations remains in development without public entries, filters, counts or feeds. Pages, environments and enabling variables were not configured; no deployment, paid call, key access, staging write, force push or history rewrite occurred.

## Owner decisions and next action

Review only the central metadata explanation, sufficient per-page provenance, five-example Delivery selection, keyboard result and readiness to publish. Architecture, Work, ideas and public voice are settled. Unresolved ORCID-only records stay withheld; no new public placeholders were introduced. Do not require a custom domain or live feed to finish this draft.

Complete the bounded Gate 5I owner review and any manual keyboard checklist. Confirm the final CI run, merge PR #1 with a merge commit, protect `main`, configure the `public-draft` environment and enabling variable, and manually publish Draft 0.1 through the guarded GitHub Pages workflow. After inspecting the live site, disable the deployment variable again. Current Conversations should remain in development.

The included runbook gives exact inputs, approval order and rollback. Later, decide independently when to review unresolved outputs, resolve external Atlas availability, consider a custom domain, and authorize live calibration. None of those later steps is automatically authorized by this handoff.
