# Current Conversations paid-discovery cost controls

Gate 4B–5A paid API cost: **CAD 0.00**. No OpenAI credential or complete owner-approved configuration was present, so the adapter failed closed before network access.

Future runs require all of: model, API key, CAD per-run and per-month ceilings, fresh USD-per-CAD conversion and date, maximum search calls/items, estimated USD per call, and a valid monthly ledger. Hard owner ceilings are CAD 2 per run and CAD 20 per month, including a 10% conversion safety margin. Missing, stale, invalid, excessive or corrupt inputs disable paid discovery.
